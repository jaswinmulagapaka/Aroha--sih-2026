// src/data/users.js
//
// TEMPORARY in-memory "database" for user profiles.
// This is just an array living in memory — it resets every time the server
// restarts. Fine for hackathon MVP purposes; swap for a real DB later.

const users = [];

module.exports = users;
